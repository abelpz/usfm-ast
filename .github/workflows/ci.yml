name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  checks:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      CI: true
    steps:
      - uses: actions/checkout@v7

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.3

      - name: Install dependencies
        # TODO: restore --frozen-lockfile once bun Windows/Linux lockfile
        # inconsistency is resolved (tracked in issue #29 notes).
        run: bun install

      - name: Lint
        run: bun run lint

      - name: Typecheck
        run: bun run check-types

      - name: Test
        env:
          RUN_RELAY_POOL_TESTS: '1'
          # smoke-test requires published npm packages; skip in workspace CI
          SMOKE_TEST_SKIP: '1'
        run: bun run test

      - name: Example USJ coverage
        run: bun run examples:check

      - name: Build
        run: bun run build

      - name: Integration tests
        run: bunx jest tests/integration
