/** Stub default export for `lucide/dist/esm/icons/*.js` under Jest (packages ship ESM only). */
const stub: [string, Record<string, string | number | undefined>][] = [
  ['circle', { cx: '12', cy: '12', r: '10' }],
];
export default stub;
