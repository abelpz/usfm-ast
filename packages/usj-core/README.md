# @usj-tools/core

USJ utilities. Currently exports **`validateUsjStructure`**: structural validation for USJ JSON (root `type: "USJ"`, `version`, `content`, and typed object nodes; string leaves allowed).
