# @usfm-tools/checking

Pure functions for the **checking** domain: review stages, checklist instances, comment threads, decisions, and **text-reference resolution** (exact / relocated / approximate / stale).

Depends only on `@usfm-tools/types`. JSON I/O helpers are format-agnostic; apps persist to `checkings/` per the enhanced project model.
