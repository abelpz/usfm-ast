# @usfm-tools/editor-themes

CSS design tokens, layout chrome, Paratext-style marker typography (`markers.css` generated from `data/usfm.sty`), and `resolveUSFMChrome` types.

Most applications should import **`@usfm-tools/editor/chrome.css`**, which pulls in `base.css` and `markers.css` from this package.
