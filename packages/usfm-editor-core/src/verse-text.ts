/**
 * Collect verse `sid` → plain text for alignment reconciliation after content edits.
 *
 * Implemented in `@usfm-tools/usj-core` for reuse outside the editor.
 */

export { collectVerseTextsFromContent } from '@usfm-tools/usj-core';
